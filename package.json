{
  "name": "modulabjs",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "compile": "tsc --build && grunt clean && grunt compile && grunt clean && cd dist && npx prettier --write *.js",
    "test": "jest --detectOpenHandles",
    "test:watch": "jest --watch --detectOpenHandles"
  },
  "author": "Ing. Erasto Marroquin Ortega",
  "private": true,
  "license": "ISC",
  "files": [
    "out"
  ],
  "dependencies": {
    "chalk": "^4.1.2"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/node": "^22.5.3",
    "class-transformer": "^0.5.1",
    "grunt": "^1.6.1",
    "grunt-template-render": "^0.0.1",
    "grunt-text-replace": "^0.4.0",
    "jest": "^29.7.0",
    "prettier": "^3.5.3",
    "ts-jest": "^29.2.5",
    "typescript": "^5.5.4"
  }
}
